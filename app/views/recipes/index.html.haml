%h1 All Recipes
- @recipes.each_slice(3) do |row|
	.row
	- row.each do |r|
		.col-sm-4
			.panel.panel-info
				.panel-heading
					.panel_title= r.title
				.panel-body
					= r.description
				.panel-footer
					= link_to "Edit", edit_recipe_path(r), class: 'btn btn-default'
					= link_to "Delete", recipe_path(r), method: :delete, class: 'btn btn-default'